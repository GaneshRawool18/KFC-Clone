import 'package:flutter/material.dart';

class MyRestaurantsAddressesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('My Restaurants & Addresses')),
      body: Center(child: Text('My Restaurants & Addresses Content')),
    );
  }
}